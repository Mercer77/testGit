<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bigwork">


    <application android:theme="@style/AppTheme" android:supportsRtl="true" android:label="@string/app_name" android:icon="@mipmap/ic_launcher" android:allowBackup="true">


    <activity android:name=".WelcomeActivity"
        android:theme="@style/Theme.Design.Light.NoActionBar">


        <intent-filter>

        <action android:name="android.intent.action.MAIN"/>

        <category android:name="android.intent.category.LAUNCHER"/>

        </intent-filter>

    </activity>

    <activity android:theme="@style/Translucent" android:name=".RegisterActivity"/>

    <activity android:name=".LoginActivity" />
    <activity android:name=".MainActivity" android:label="交易中心"/>
     <activity android:name=".ShowGoods" android:label="商品详情" />
        <activity android:name=".ShowCollectionXx" />
        <activity android:name=".ShowCollection" android:label="我的收藏"  />
        <activity android:name=".ShowChangeGoods" android:label="商品详情"/>
</application>

</manifest>